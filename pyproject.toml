[project]
name = "psml_benchmark"
version = "0.1.0"
requires-python = ">=3.10"
description = "PSML benchmark components"

[tool.setuptools]
package-dir = {"" = "Code"}
py-modules = ["dataloader"]

[tool.setuptools.packages.find]
where = ["Code"]